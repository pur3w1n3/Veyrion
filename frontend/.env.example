VITE_DEMO_MODE=true
VITE_API_BASE_URL=/api/v1
VITE_PROJECT_ID=project-01
# Optional for REST mutations. Prefer an HttpOnly same-origin session for SSE.
VITE_API_TOKEN=
# Optional; dashboard.scanId is preferred when the server supplies it.
VITE_SCAN_ID=
