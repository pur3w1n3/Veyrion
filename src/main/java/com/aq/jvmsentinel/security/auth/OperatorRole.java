package com.aq.jvmsentinel.security.auth;

/** Human/operator roles. These roles are never assigned to Worker credentials. */
public enum OperatorRole {
    VIEWER,
    ANALYST,
    OPERATOR,
    ADMINISTRATOR
}
